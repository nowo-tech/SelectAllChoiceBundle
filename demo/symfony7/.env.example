# Demo environment template. Copy to .env (e.g. cp .env.example .env) and adjust.
# Do not commit .env. Required: APP_ENV, APP_SECRET, APP_DEBUG, PORT.
APP_ENV=dev
APP_DEBUG=1
APP_SECRET=change_this_secret_key_to_a_random_value
PORT=8007
