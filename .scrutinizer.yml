checks:
  php:
    code_rating: true
    duplication: true

filter:
  excluded_paths:
    - "demo/*"
    - "tests/*"
    - "vendor/*"
    - "node_modules/*"

build:
  environment:
    php:
      version: 8.2
      ini:
        memory_limit: -1
        'xdebug.mode': coverage
    node:
      # Vite 6 / Vitest 2 expect a current Node LTS; Scrutinizer images do not ship pnpm on PATH.
      version: 20

  cache:
    directories:
      - vendor/

  tests:
    before:
      - composer validate --strict
      - composer install --no-interaction --prefer-dist --optimize-autoloader
      # Scrutinizer provides npm/node but not pnpm; install before lockfile install + Vitest.
      - npm install -g pnpm
    override:
      - command: composer test-coverage
        environment:
          XDEBUG_MODE: coverage
        coverage:
          file: coverage.xml
          format: clover
      - command: pnpm install --frozen-lockfile && pnpm run test
